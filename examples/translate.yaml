service:
  port: 8080
  host: "localhost"
  db: "mysql"

db:
  mysql:
    host: "localhost"
    port: 3306
    user: "root"
    password: ""
    database: "translate"
  badgerdb:
    path: "/tmp/badgerdb"
